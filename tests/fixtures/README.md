# Synthetic fixtures

Tests generate entirely fictional DOCX fixtures in temporary directories. No real user,
client, employer, or research documents are stored here. Temporary fixtures are deleted
automatically after each test.
